# Пустой URL — запросы на localhost:5173/api, Vite проксирует на удалённый бэкенд.
# Так cookies работают в dev (бэкенд отдаёт Secure, proxy снимает флаг).
VITE_API_URL=http://localhost:8000
VITE_API_PROXY_TARGET=http://localhost:8000
